# Base de datos (Postgres en Docker, ver docker-compose.yml)
DATABASE_URL="postgresql://spot:spot_dev_pw@localhost:5433/spot?schema=public"

# Auth
JWT_SECRET="cambiar-esto-por-una-clave-larga-y-random-en-prod"
JWT_EXPIRES_IN="7d"

# API
API_PORT=3001

# Frontend (Expo) — apunta al API
EXPO_PUBLIC_API_URL="http://localhost:3001"
